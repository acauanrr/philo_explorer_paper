# API Configuration
# Node.js API endpoint (handles all requests including ML service calls)
NEXT_PUBLIC_API_URL=http://localhost:6001

# Note: ML Service (https://acauanrr-phylo-ml-service.hf.space) is accessed
# through the backend API, not directly from the frontend

# Application Configuration
NEXT_PUBLIC_APP_NAME=Phylo Explorer
NEXT_PUBLIC_APP_VERSION=1.0.0

# Optional: Legacy API endpoints (for backward compatibility)
# NEXT_PUBLIC_API_URL_LOCAL=http://localhost:6001/
# NEXT_PUBLIC_API_URL_DEPLOY=https://phylo-explorer-api.herokuapp.com/